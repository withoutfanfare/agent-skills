# Documentation plan: <work item>

Written by `work document`. One row for each audience the delivery profile
says this type of change needs. Each page is written in plain English, with
`plainify` when installed.

| Audience | `plainify` purpose | Built from | Saved to | Status |
| --- | --- | --- | --- | --- |
| Engineers extending it | how-it-fits guide for newcomers | code paths, the pull request, the brief | `<path>` | `Drafted` / `Saved` / `Not needed (reason)` |
| Admins and customers | how-to guide | screenshots, the UAT plan | `<path>` | |
| Whoever runs it | runbook page | release checklist, existing runbook | `<path>` | |

Sources read: `<list>`. Nothing beyond the sources was invented; gaps are
marked "Unverified" in the page itself.
